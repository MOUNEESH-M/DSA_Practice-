class Solution {
    public int maxSubArray(int[] nums) {
        int sum=0;
        int msum=0;
        for(int i:nums){
            sum=Math.max(i,sum+i);
            msum=Math.max(sum,msum);
        }
        return msum;
    }
}
